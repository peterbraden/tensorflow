{
  "name": "tensorflow",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "nan": "^2.3.2"
  },
  "devDependencies": {
    "tape": "^4.5.1"
  },
  "scripts": {
    "generate": "./generate.sh",
    "install": "node-gyp --build_dir=node_build clean && ./prebuild.sh && node-gyp --build_dir=node_build --verbose configure && node-gyp --build_dir=node_build --verbose build && ./postbuild.sh",
    "test": "node test/suite.js"
  },
  "license": "Apache-2.0"
}
